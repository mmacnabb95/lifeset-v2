import constants from "../constants";

export default {
  primaryColor: constants.primaryColor,
  textColor: constants.black,
  checkedTextColor: constants.primaryColor,
  iconColor: constants.grey800,
  checkedIconColor: constants.primaryColor,
};
