// STUB: Stripe integration removed - will use RevenueCat in Phase 5
import React from "react";
import { View, Text } from "react-native";

const StripeNativeCheckoutScreen = () => {
  return (
    <View style={{ padding: 20 }}>
      <Text>Stripe checkout removed. RevenueCat integration coming in Phase 5.</Text>
    </View>
  );
};

export default StripeNativeCheckoutScreen;
